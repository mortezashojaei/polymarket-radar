services:
  radar:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER:-owner}/polymarket-radar:latest
    container_name: polymarket-radar
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
