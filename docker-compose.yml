services:
  radar:
    image: ghcr.io/mortezashojaei/polymarket-radar:latest
    container_name: polymarket-radar
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
